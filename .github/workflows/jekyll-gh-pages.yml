# Legacy workflow kept for reference only.
# The app is deployed by `.github/workflows/static.yml`.
name: (Disabled) Legacy Jekyll Pages workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Jekyll deployment is disabled. Use the static Vite Pages workflow instead."
