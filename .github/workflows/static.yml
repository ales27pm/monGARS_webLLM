# Build + deploy the Vite dist/ bundle to GitHub Pages (Actions-based Pages)
name: Deploy monGARS_webLLM to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Exposes useful outputs such as base_path ("/<repo>/" for project pages, "/" for user pages/custom domains)
      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v5

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install deps
        run: npm ci

      - name: Build (Vite)
        env:
          BASE_PATH: ${{ steps.pages.outputs.base_path }}
        run: |
          echo "Using Vite base path: ${BASE_PATH}"
          npm run build -- --base="${BASE_PATH}"
          if [ ! -f dist/404.html ]; then cp dist/index.html dist/404.html; fi
          touch dist/.nojekyll

      - name: Verify Pages bundle
        run: |
          test -f dist/index.html || { echo "ERROR: missing dist/index.html"; exit 1; }
          test -f dist/404.html || { echo "ERROR: missing dist/404.html"; exit 1; }
          test -f dist/.nojekyll || { echo "ERROR: missing dist/.nojekyll"; exit 1; }

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
